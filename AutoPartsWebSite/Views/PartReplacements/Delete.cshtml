@model AutoPartsWebSite.Models.PartReplacement

@{
    ViewBag.Title = "УДАЛЕНИЕ ЗАМЕНЫ";
}

<DIV class="main-text">
<h1 id="cntr">@ViewBag.Title</h1>
<div class="reg-form">
<br>

<h3>Внимание!</h3>
<p>Номер: <span id="red">@Html.DisplayFor(model => model.Number)</span></p>
<p>Замена: @Html.DisplayFor(model => model.Replacement)</p>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="УДАЛИТЬ" class="btn btn-default" />
        </div>
    }
</div>
</div>
<div id="cntr">
@Html.ActionLink("Назад в таблицу замен", "Index")
</div><br>
